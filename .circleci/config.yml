version: 2.1

jobs:
  build:
    docker:
      - image: 'debian:10-slim'
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: 'apt update && apt install -y git build-essential cmake'
      - run:
          name: Build project
          command: 'cmake . && sh cmake --build .'
      - run:
          name: Update git-submodules
          command: 'git submodule update --init --recursive'
      - sonarcloud/scan

orbs:
  sonarcloud: sonarsource/sonarcloud@1.0.1

workflows:
  main:
    jobs:
      - build:
          context: SonarCloud
